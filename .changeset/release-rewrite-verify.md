---
"@moonshot-ai/kimi-code": patch
---

Verify the release workflow's fork-identity rewrite: changeset package references are rewritten from the upstream package name to `@vyl/kimi-code` before the version step runs.
